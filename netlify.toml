# Netlify configuration for the De La Hunt campaign site.
# The site is plain static HTML/CSS (no build step), so there is nothing to compile.

[build]
  publish = "public"

# Security response headers applied to every page.
# Styles are inline and images are local files or data: URIs. The candidate-forum
# video is embedded from Facebook, so only that frame origin is allowed.
[[headers]]
  for = "/*"
  [headers.values]
    Content-Security-Policy = "default-src 'self'; img-src 'self' data:; style-src 'self' 'unsafe-inline'; font-src 'self'; frame-src https://www.facebook.com; form-action 'self'; frame-ancestors 'none'; base-uri 'self'; object-src 'none'"
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "geolocation=(), camera=(), microphone=(), payment=(), usb=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains; preload"
